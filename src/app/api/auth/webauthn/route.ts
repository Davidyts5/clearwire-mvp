export const dynamic = "force-dynamic";
// Placeholder for Phase 2: Supabase Auth & WebAuthn Registration
export async function GET() {
  return new Response("Auth endpoint ready for Phase 2", { status: 200 });
}
