# ClearWire MVP
B2B Anti-Deepfake Wire Fraud Protection.

## Schema Migrations

Any future schema change should be a new, clearly-named, timestamped file added directly to `src/lib/` (not the archive). Before running anything against production, you must read the file's own comments to confirm it's additive (CREATE/ALTER), not destructive (DROP/TRUNCATE).
